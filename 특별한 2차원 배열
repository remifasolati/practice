def solution(arr):
    l = len(arr)
    for i in range(l):
        for j in range(l):
            if arr[i][j]!=arr[j][i]:
                return 0
    return 1
      
  # return int(all([1 if arr[i][j]==arr[j][i] else 0 for i in range(len(arr)) for j in range(len(arr))]))
